<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - CMCS</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }

        /* The media query syntax is now correct for CSS */
        @@media (min-width: 640px) {
            body {
                background-color: #f9fafb;
            }
        }
    </style>
</head>
<body>
    <header class="bg-white shadow-lg">
        <nav class="container mx-auto px-6 py-3 flex items-center justify-between">
            <a class="text-2xl font-bold text-gray-800" asp-area="" asp-controller="Home" asp-action="Index">CMCS</a>
            <div class="flex items-center space-x-4">
                <a class="text-gray-600 hover:text-indigo-600 transition-colors duration-200" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                <a class="text-gray-600 hover:text-indigo-600 transition-colors duration-200" asp-area="" asp-controller="Home" asp-action="SubmitClaim">Submit Claim</a>
                <a class="text-gray-600 hover:text-indigo-600 transition-colors duration-200" asp-area="" asp-controller="Home" asp-action="ApproveClaims">Approve Claims</a>
                <a class="text-gray-600 hover:text-indigo-600 transition-colors duration-200" asp-area="" asp-controller="Account" asp-action="Login">Login</a>
                <a class="text-gray-600 hover:text-indigo-600 transition-colors duration-200" asp-area="" asp-controller="Account" asp-action="Logout">Logout</a>
                <a class="text-gray-600 hover:text-indigo-600 transition-colors duration-200" asp-area="" asp-controller="Account" asp-action="Register">Register</a>
            </div>
        </nav>
    </header>
    <div class="container mx-auto mt-8 px-6">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-t mt-8 py-4 text-center text-gray-500 text-sm">
        <div class="container mx-auto">
            &copy; 2024 - CMCS - <a class="text-indigo-600 hover:underline" asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
        </div>
    </footer>
</body>
</html>
